class Cheetah {
    makeSound() {
        console.log('meow meow');
    }
    move(distance = 0) {
        console.log(distance);
    }
}
class FooClass {
    bar(arg) {
        if (typeof arg === 'number')
            return arg.toString();
        if (typeof arg === 'string')
            return arg.length;
    }
}
//# sourceMappingURL=03_implementInterface.js.map